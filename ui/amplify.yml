version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build:prod
      artifacts:
        baseDirectory: /dist/user-management
        files:
          - "**/*"
      cache:
        paths: 
          - node_modules/**/*
    appRoot: /ui
